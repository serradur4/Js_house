function arrayPrinting(numbers){
    for(let i = 0; i < numbers.length; i++){    //stampa un'array
        console.log(numbers[i]);
    }
}
const numbers =  [23, 24, 25, 26];
const print = arrayPrinting(numbers)
